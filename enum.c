#include<stdio.h>
#include<string.h>
enum color {RED, GREEN, BLUE};
int main()
{
  enum sakib {valo, kharap};
  enum color c;
  c = RED;
  printf("Value of c: %d\n", c);

  enum sakib s;
  s = valo;
  printf("Value of s: %d\n", s);
  
}